/**
 * Vite entry kept minimal — production assets live in public/ (CDN + static JS).
 * Use this file only if a page loads @vite(['resources/js/app.js']).
 */
console.debug('Vite app entry loaded');
